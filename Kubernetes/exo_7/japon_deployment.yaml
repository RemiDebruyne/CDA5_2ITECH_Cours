apiVersion: apps/v1
kind: Deployment

metadata:
  name: japon-deployment

spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      name: japon-pod
      labels:
        app: nginx
    spec:
      tolerations:
        - key: 'country'
          operator: 'Equal'
          value: 'japon'
          effect: 'NoSchedule'
      containers:
        - name: japon-container
          image: nginx:alpine
          resources:
            requests:
              memory: '64Mi'
              cpu: '100m'
            limits:
              memory: '128Mi'
              cpu: '200m'
