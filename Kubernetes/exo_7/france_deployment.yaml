apiVersion: apps/v1
kind: Deployment

metadata:
  name: france-deployment

spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      name: france-pod
      labels:
        app: nginx
    spec:
      tolerations:
        - key: 'country'
          operator: 'Equal'
          value: 'france'
          effect: 'NoSchedule'
      containers:
        - name: france-container
          image: nginx:alpine
          resources:
            requests:
              memory: '64Mi'
              cpu: '100m'
            limits:
              memory: '128Mi'
              cpu: '200m'
